var define = require("node-constants")(exports);


////////////////////////////////////////////////////////////////////////

define({
      check_inputs : {
        failure_1 : 'Please Check the inputs' ,
        failure_2 : 'input type in not correct' ,
    }
});
